{
  "name": "clear",
  "version": "1.9.0",
  "private": true,
  "type": "module",
  "description": "A private log for bronchiectasis. All data stays on the user's device.",
  "scripts": {
    "build": "node build/build.mjs"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
